Running example code:
```
cd lox_code
python3 ../lox.py huffman.lox
python3 ../lox.py challenges.lox
```

Running tests:
```
python3 -m venv .venv
. ./.venv/bin/activate
pip3 install -r requirements.txt
pytest
```
I got lazy with testing. Too excited to work on the clox implementation.
